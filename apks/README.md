# APK files

Add your final APK files in this folder using these exact filenames:

- `ChargeGuard.apk`
- `QRcraft.apk`
- `RelayOTP.apk`

The website download buttons already point to:

- `apks/ChargeGuard.apk`
- `apks/QRcraft.apk`
- `apks/RelayOTP.apk`

Do not rename them unless you also update the links in the HTML files.
